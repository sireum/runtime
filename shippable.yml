language: none

services:

build:
  pre_ci:
  pre_ci_boot:
    image_name: sireum/v3
    image_tag: latest
    pull: true
    options: "-e HOME=/root"
  ci:
    - ./prelude.sh
    - ./mill-standalone jitPack --owner sireum --repo runtime --lib library || true
    - ./test.sh